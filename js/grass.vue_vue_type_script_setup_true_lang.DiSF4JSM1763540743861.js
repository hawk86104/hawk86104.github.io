import{c as t}from"./simplex-noise.7LStyxTL1763540743861.js";import{_ as e}from"./@tresjs.BCHtwiaC1763540743861.js";import{C as n,d as o}from"./index.Crdf3MNx1763540743861.js";import{d as a,a8 as i,H as r,o as s,J as l,u as c}from"./@vue.Co_gxueH1763540743861.js";import{o as h,V as f,l as u,ae as v,A as d,ak as m}from"./three.rXKzP9fQ1763540743861.js";const p=["material"],x=["index","attributes-position","attributes-uv","attributes-offset","attributes-orientation","attributes-stretch","attributes-halfRootAngleSin","attributes-halfRootAngleCos"],g=["geometry"],w=["side"],y=a({__name:"grass",props:{bW:{default:.12},bH:{default:1},joints:{default:5},width:{default:100},instances:{default:5e4}},async setup(a){let y,b;const M=([y,b]=i(()=>o(["./plugins/floor/image/blade_diffuse.jpg","./plugins/floor/image/blade_alpha.jpg"])),y=await y,b(),y),_=a,z=t(Math.random);function A(t,e){const n=t.x*e.w+t.y*e.z-t.z*e.y+t.w*e.x,o=-t.x*e.z+t.y*e.w+t.z*e.x+t.w*e.y,a=t.x*e.y-t.y*e.x+t.z*e.w+t.w*e.z,i=-t.x*e.x-t.y*e.y-t.z*e.z+t.w*e.w;return new d(n,o,a,i)}function C(t,e){var n=2*z(t/50,e/50);return n+=4*z(t/100,e/100),n+=.2*z(t/10,e/10)}const F=function(t,e){const n=[],o=[],a=[],i=[],r=[];let s=new d,l=new d;const c=-.25;for(let h=0;h<t;h++){const u=Math.random()*e-e/2,v=Math.random()*e-e/2,d=C(u,v);n.push(u,d,v);let m=Math.PI-Math.random()*(2*Math.PI);i.push(Math.sin(.5*m)),r.push(Math.cos(.5*m));let p=new f(0,1,0),x=p.x*Math.sin(m/2),g=p.y*Math.sin(m/2),w=p.z*Math.sin(m/2),y=Math.cos(m/2);s.set(x,g,w,y).normalize(),m=.5*Math.random()+c,p=new f(1,0,0),x=p.x*Math.sin(m/2),g=p.y*Math.sin(m/2),w=p.z*Math.sin(m/2),y=Math.cos(m/2),l.set(x,g,w,y).normalize(),s=A(s,l),m=.5*Math.random()+c,p=new f(0,0,1),x=p.x*Math.sin(m/2),g=p.y*Math.sin(m/2),w=p.z*Math.sin(m/2),y=Math.cos(m/2),l.set(x,g,w,y).normalize(),s=A(s,l),o.push(s.x,s.y,s.z,s.w),h<t/3?a.push(1.8*Math.random()):a.push(Math.random())}return{offsetsF32:new m(new Float32Array(n),3),orientationsF32:new m(new Float32Array(o),4),stretchesF32:new m(new Float32Array(a),1),halfRootAngleCosF32:new m(new Float32Array(r),1),halfRootAngleSinF32:new m(new Float32Array(i),1)}}(_.instances,_.width),S=new h(_.bW,_.bH,1,_.joints).translate(0,_.bH/2,0),R=new h(_.width,_.width,32,32);R.lookAt(new f(0,1,0));const D=R.attributes.position;for(let t=0;t<D.array.length;t+=3)D.array[t+1]=C(D.array[t],D.array[t+2]);R.attributes.position.needsUpdate=!0,R.computeVertexNormals();const T=new(n({bladeHeight:1,map:null,alphaMap:null,time:0,tipColor:new u(.3,.9,0).convertSRGBToLinear(),bottomColor:new u(0,.2,0).convertSRGBToLinear()},"   precision mediump float;\n      attribute vec3 offset;\n      attribute vec4 orientation;\n      attribute float halfrootanglesin;\n      attribute float halfrootanglecos;\n      attribute float stretch;\n      uniform float time;\n      uniform float bladeHeight;\n      varying vec2 vUv;\n      varying float frc;\n\n      //WEBGL-NOISE FROM https://github.com/stegu/webgl-noise\n      //Description : Array and textureless GLSL 2D simplex noise function. Author : Ian McEwan, Ashima Arts. Maintainer : stegu Lastmod : 20110822 (ijm) License : Copyright (C) 2011 Ashima Arts. All rights reserved. Distributed under the MIT License. See LICENSE file. https://github.com/ashima/webgl-noise https://github.com/stegu/webgl-noise\n      vec3 mod289(vec3 x) {return x - floor(x * (1.0 / 289.0)) * 289.0;} vec2 mod289(vec2 x) {return x - floor(x * (1.0 / 289.0)) * 289.0;} vec3 permute(vec3 x) {return mod289(((x*34.0)+1.0)*x);} float snoise(vec2 v){const vec4 C = vec4(0.211324865405187, 0.366025403784439, -0.577350269189626, 0.024390243902439); vec2 i  = floor(v + dot(v, C.yy) ); vec2 x0 = v -   i + dot(i, C.xx); vec2 i1; i1 = (x0.x > x0.y) ? vec2(1.0, 0.0) : vec2(0.0, 1.0); vec4 x12 = x0.xyxy + C.xxzz; x12.xy -= i1; i = mod289(i); vec3 p = permute( permute( i.y + vec3(0.0, i1.y, 1.0 )) + i.x + vec3(0.0, i1.x, 1.0 )); vec3 m = max(0.5 - vec3(dot(x0,x0), dot(x12.xy,x12.xy), dot(x12.zw,x12.zw)), 0.0); m = m*m ; m = m*m ; vec3 x = 2.0 * fract(p * C.www) - 1.0; vec3 h = abs(x) - 0.5; vec3 ox = floor(x + 0.5); vec3 a0 = x - ox; m *= 1.79284291400159 - 0.85373472095314 * ( a0*a0 + h*h ); vec3 g; g.x  = a0.x  * x0.x  + h.x  * x0.y; g.yz = a0.yz * x12.xz + h.yz * x12.yw; return 130.0 * dot(m, g);}\n      //END NOISE\n\n      //https://www.geeks3d.com/20141201/how-to-rotate-a-vertex-by-a-quaternion-in-glsl/\n      vec3 rotateVectorByQuaternion( vec3 v, vec4 q){\n        return 2.0 * cross(q.xyz, v * q.w + cross(q.xyz, v)) + v;\n      }\n\n      //https://en.wikipedia.org/wiki/Slerp\n      vec4 slerp(vec4 v0, vec4 v1, float t) {\n        // Only unit quaternions are valid rotations.\n        // Normalize to avoid undefined behavior.\n        normalize(v0);\n        normalize(v1);\n\n        // Compute the cosine of the angle between the two vectors.\n        float dot_ = dot(v0, v1);\n\n        // If the dot product is negative, slerp won't take\n        // the shorter path. Note that v1 and -v1 are equivalent when\n        // the negation is applied to all four components. Fix by\n        // reversing one quaternion.\n        if (dot_ < 0.0) {\n          v1 = -v1;\n          dot_ = -dot_;\n        }\n\n        const float DOT_THRESHOLD = 0.9995;\n        if (dot_ > DOT_THRESHOLD) {\n          // If the inputs are too close for comfort, linearly interpolate\n          // and normalize the result.\n          vec4 result = t*(v1 - v0) + v0;\n          normalize(result);\n          return result;\n        }\n\n        // Since dot is in range [0, DOT_THRESHOLD], acos is safe\n        float theta_0 = acos(dot_);       // theta_0 = angle between input vectors\n        float theta = theta_0*t;          // theta = angle between v0 and result\n        float sin_theta = sin(theta);     // compute this value only once\n        float sin_theta_0 = sin(theta_0); // compute this value only once\n        float s0 = cos(theta) - dot_ * sin_theta / sin_theta_0;  // == sin(theta_0 - theta) / sin(theta_0)\n        float s1 = sin_theta / sin_theta_0;\n        return (s0 * v0) + (s1 * v1);\n      }\n\n      void main() {\n        //Relative position of vertex along the mesh Y direction\n        frc = position.y/float(bladeHeight);\n        //Get wind data from simplex noise\n        float noise = 1.0-(snoise(vec2((time-offset.x/50.0), (time-offset.z/50.0))));\n        //Define the direction of an unbent blade of grass rotated around the Y axis\n        vec4 direction = vec4(0.0, halfrootanglesin, 0.0, halfrootanglecos);\n        //Interpolate between the unbent direction and the direction of growth calculated on the CPU.\n        //Using the relative location of the vertex along the Y axis as the weight, we get a smooth bend\n        direction = slerp(direction, orientation, frc);\n        vec3 vPosition = vec3(position.x, position.y + position.y * stretch, position.z);\n        vPosition = rotateVectorByQuaternion(vPosition, direction);\n\n       //Apply wind\n       float halfAngle = noise * 0.15;\n        vPosition = rotateVectorByQuaternion(vPosition, normalize(vec4(sin(halfAngle), 0.0, -sin(halfAngle), cos(halfAngle))));\n        //UV for texture\n        vUv = uv;\n        //Calculate final position of the vertex from the world offset and the above shenanigans\n        gl_Position = projectionMatrix * modelViewMatrix * vec4(offset + vPosition, 1.0 );\n      }","\n      precision mediump float;\n      uniform sampler2D map;\n      uniform sampler2D alphaMap;\n      uniform vec3 tipColor;\n      uniform vec3 bottomColor;\n      varying vec2 vUv;\n      varying float frc;\n\n      void main() {\n        //Get transparency information from alpha map\n        float alpha = texture2D(alphaMap, vUv).r;\n        //If transparent, don't draw\n        if(alpha < 0.15) discard;\n        //Get colour data from texture\n        vec4 col = vec4(texture2D(map, vUv));\n        //Add more green towards root\n        col = mix(vec4(tipColor, 1.0), col, frc);\n        //Add a shadow towards root\n        col = mix(vec4(bottomColor, 1.0), col, frc);\n        gl_FragColor = col;\n\n        #include <tonemapping_fragment>\n\t      #include <colorspace_fragment>\n      }",t=>{t.side=v}));T.map=M[0],T.alphaMap=M[1],T.toneMapped=!1;const{onRender:H}=e();return H(({elapsed:t})=>{T.uniforms.time.value=t/4}),(t,e)=>(s(),r("TresGroup",null,[l("TresMesh",{material:c(T)},[l("TresInstancedBufferGeometry",{index:c(S).index,"attributes-position":c(S).attributes.position,"attributes-uv":c(S).attributes.uv,"attributes-offset":c(F).offsetsF32,"attributes-orientation":c(F).orientationsF32,"attributes-stretch":c(F).stretchesF32,"attributes-halfRootAngleSin":c(F).halfRootAngleSinF32,"attributes-halfRootAngleCos":c(F).halfRootAngleCosF32},null,8,x)],8,p),l("TresMesh",{position:[0,0,0],geometry:c(R)},[l("TresMeshStandardMaterial",{color:"#000f00",side:v},null,8,w)],8,g)]))}});export{y as _};
